# Worker container dependencies (the worker is NOT a Lambda, so it bundles its own).
requests==2.34.2
pymysql==1.1.1
# Required by PyMySQL for MySQL 8 / RDS "caching_sha2_password" auth.
cryptography==48.0.0
# ORM. Alembic is used in-memory by sync_database() (diff models -> DB); the
# worker never syncs on boot - schema sync is triggered by hand via `make sync`.
SQLAlchemy==2.0.36
alembic==1.13.2
boto3==1.34.100
# IANA tz database for zoneinfo (required on Windows dev machines).
tzdata>=2024.1
