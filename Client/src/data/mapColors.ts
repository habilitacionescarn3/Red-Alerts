/** Semantic map layer colors shared across live map, pins, and geo admin preview. */
export const MAP_COLORS = {
  active: '#ef4444',
  recent: '#f59e0b',
  selectedStroke: '#ffffff',
  selectedFill: '#ffffff',
  geoCurrent: '#f59e0b',
  geoCandidate: '#10b981',
} as const;
